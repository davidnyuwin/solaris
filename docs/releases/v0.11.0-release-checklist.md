# Solaris v0.11.0 — Release Checklist

**Package under test:** `Solaris-v0.11.0-beta.zip`  
**Commit:** `8584feb6808e9df174da7d1967b8f9a464e2bd9c`  
**Date tested:** June 5, 2026  
**Tester:** David  
**Manual Smoke Status:** Passed  

---

## 1. Automated Validation (CI Gates)

Ensure the latest GitHub Actions CI run for the release candidate commit is green.

- [x] **Build & Verify Job** passes.
  - Compile Solaris (Debug) passes.
  - Run Unit Tests passes (227 tests run, 0 failures).
  - Run API Smoke Test passes.
  - Run Security & Secret Scan (`./scripts/secret-scan.sh`) passes.
- [x] **Package & Code Sign Job** passes.
  - App signing verified (`codesign --verify --deep --strict`).
  - Production zip artifact exists (`Solaris-v0.11.0-beta.zip`).
- [x] **Release Build Compilation** compiles cleanly locally:
  ```bash
  swift build -c release
  ```
- [x] **Smoke Test Script** executes cleanly:
  ```bash
  ./scripts/smoke-test.sh
  ```
- [x] **Pre-commit Scan** executes cleanly with zero warnings:
  ```bash
  ./scripts/secret-scan.sh
  ```

---

## 2. Manual Smoke & Visual Parity (Physical Device)

> [!IMPORTANT]
> The automated pipeline does not perform visual checks. A developer must run the packaged release bundle (`dist/Solaris.app`) on macOS and physically verify the interface.

### UI Layout & Controls
- [x] **App Launch**: Solaris boots cleanly with no unexpected crashes or terminal windows.
- [x] **Settings Layout**: Visual elements under Settings -> Remote Host Mode align correctly.
- [x] **Mock Mode**: Toggling to Mock Mode functions correctly; recent runs and provider lists populate instantly.
- [x] **Local Diagnostics**: Clicking "Refresh Diagnostics" (Command R) triggers a refresh and populates local process states.

### Safety Opt-in Gates
- [x] **Policy Disabled State**: Toggle for "Enable Live Remote Probes" starts **off** by default. Setting host or testing connection returns `Live remote checks are disabled` message.
- [x] **Opt-In Alert Dialog**: Toggling "Enable Live Remote Probes" on displays the approval modal warning.
- [x] **Cancel Opt-In**: Clicking Cancel inside the modal dismisses the dialog and keeps the toggle disabled.
- [x] **Approved Opt-In**: Clicking "Enable Read-Only Probes" updates the policy, saves the approved key, and updates status labels.
- [x] **Disable and Clear**: Toggling the switch off clears approval (`LiveRemotePolicyUserApproved = false`), clears policy, and resets remote status instantly.

### Live Read-Only Probes
- [x] **Probe Outcome**: Running probes against a valid mock or real remote host updates `findHermesBinary`, `hermesVersion`, `hermesStatus`, and `tunnelStatus` correctly.
- [x] **Path Redaction**: Full bin paths are ignored and only boolean values display.
- [x] **Release Blocked Controls**: Chat panel, Daemon Restart button, and Tunnel Start/Stop elements display "This operation is disabled in release builds" or remain locked.

### Diagnostic Logging & Filters
- [x] **Console Display**: Logs correctly populate in the parsed system log panel.
- [x] **Source Badges**: Logs carry clear, colored source labels (`APP`, `MOCK`, `LIVEPROBE`, etc.).
- [x] **Filter Menu**: Selecting a source or severity successfully filters the console output.
- [x] **Redacted Only**: Toggling the Redacted checkbox displays only the entries that contain redacted data.
- [x] **Copy/Export Summary**: Copying or exporting visible logs to a text file produces cleanly-formatted, privacy-redacted summaries.

### UI Polish & Aesthetics
- [x] **Layout Parity**: No visual clipping, truncated text boxes, or overlapping badge elements.
- [x] **No Hangs/Freezes**: Navigating menus, pausing/resuming log feeds, and switching tabs is fluid and instantaneous.
